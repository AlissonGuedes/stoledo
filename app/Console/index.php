#!/usr/bin/php
<?php

// $function = isset($function) ? $function() : die();
// $registro = isset($registro) ? $registro : null;

function inserirFornecedor($registro) {

	// global $registro;

	echo $registro;

}

// get_defined_constants();